## PR 说明 / Description

<!-- 请简要描述你的修改内容 / Please describe your changes briefly -->

## 目标分支 / Target Branch 
> **重要 / Important**
> 所有功能更新和修复请提交到 **`dev`** 分支，`main` 分支仅用于发布稳定版本。
> ALL feature updates and bug fixes should target the **`dev`** branch.

- [ ] 我确认此 PR 提交到 `dev` 分支，而不是 `main` 分支。 / I confirm this PR is submitted to the `dev` branch, not `main`.

## 检查单 / Checklist 
*(请在提交前完成以下检查，并将 `[ ]` 替换为 `[x]`) / (Please complete the following checks before submitting, replace `[ ]` with `[x]`)*

- [ ] **本地编译通过** / Passed local compilation (`cargo build`).
- [ ] **交叉编译通过** / Passed cross-compilation (测试了相关的目标平台如 `armv7` 或 `aarch64`).
- [ ] **功能测试通过** / Passed functional testing (已进行实际硬件/功能测试，逻辑正常).
- [ ] 修改涉及新功能或变更时，已**更新相关文档** (如 `README.md`, `docs/`) / Updated documentation for any new features or changes.
- [ ] 代码修改符合 `docs/CONTRIBUTING.md` 的规范 / Changes comply with `docs/CONTRIBUTING.md`.

## 附加说明 / Additional Notes

<!-- 截图、运行日志或其他任何有助于 Review 的附加信息 / Any screenshots, logs, or other info that might be helpful -->
